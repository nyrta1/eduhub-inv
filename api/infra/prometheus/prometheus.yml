global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts/security.yml

scrape_configs:
  - job_name: student-platform-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8000
